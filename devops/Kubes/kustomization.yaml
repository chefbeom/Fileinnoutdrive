# Legacy deployment artifact: do not deploy directly. Use devops/Helm as the source of truth.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Intentionally empty. This legacy directory is not a deployment source.
resources: []