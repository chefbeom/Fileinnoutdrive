{{- $frontendRollout := .Values.frontend.rollout | default dict -}}
{{- $frontendBlueGreen := $frontendRollout.blueGreen | default dict -}}
apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: {{ include "wafflebear.fullname" . }}-frontend
  labels:
    app.kubernetes.io/name: frontend
    app.kubernetes.io/part-of: wafflebear
spec:
  replicas: {{ .Values.frontend.replicaCount }}
  revisionHistoryLimit: 5
  progressDeadlineSeconds: 600
  minReadySeconds: 5
  selector:
    matchLabels:
      app.kubernetes.io/name: frontend
      app.kubernetes.io/part-of: wafflebear
  strategy:
    blueGreen:
      activeService: {{ include "wafflebear.fullname" . }}-frontend
      previewService: {{ include "wafflebear.fullname" . }}-frontend-preview
      autoPromotionEnabled: {{ default true $frontendBlueGreen.autoPromotionEnabled }}
      scaleDownDelaySeconds: {{ default 300 $frontendBlueGreen.scaleDownDelaySeconds }}
  template:
    metadata:
      annotations:
        checksum/frontend-nginx: {{ include (print $.Template.BasePath "/frontend-configmap.yaml") . | sha256sum }}
      labels:
        app.kubernetes.io/name: frontend
        app.kubernetes.io/part-of: wafflebear
    spec:
{{ include "wafflebear.workerScheduling" (dict "app.kubernetes.io/name" "frontend" "app.kubernetes.io/part-of" "wafflebear") | nindent 6 }}
      terminationGracePeriodSeconds: 30
      containers:
        - name: frontend
          image: "{{ .Values.frontend.image.repository }}:{{ .Values.frontend.image.tag }}"
          imagePullPolicy: {{ .Values.frontend.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.frontend.service.port }}
              name: http
          volumeMounts:
            - name: nginx-config
              mountPath: /etc/nginx/conf.d/default.conf
              subPath: default.conf
          startupProbe:
            tcpSocket:
              port: {{ .Values.frontend.service.port }}
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 30
          readinessProbe:
            httpGet:
              path: /
              port: {{ .Values.frontend.service.port }}
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 2
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /
              port: {{ .Values.frontend.service.port }}
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
          lifecycle:
            preStop:
              exec:
                command:
                  - sh
                  - -c
                  - sleep 5
          resources:
{{ toYaml .Values.frontend.resources | indent 12 }}
      volumes:
        - name: nginx-config
          configMap:
            name: {{ include "wafflebear.fullname" . }}-frontend-nginx
