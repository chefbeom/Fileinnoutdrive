# Legacy Deployment Artifact

Do not deploy this chart directly.

`devops/Helm` is the only deployment source of truth. This directory remains only as a compatibility reference and is intentionally blocked from rendering by `templates/legacy-blocker.yaml`.
