# Legacy deployment artifact: do not deploy directly. Use devops/Helm as the source of truth.
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "wafflebear.fullname" . }}-websocket-env
data:
{{- tpl (toYaml .Values.websocket.env) . | nindent 2 }}
