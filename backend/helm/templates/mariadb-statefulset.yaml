apiVersion: v1
kind: Service
metadata:
  name: mariadb
  labels:
    app: mariadb-stateful
spec:
  clusterIP: None
  selector:
    app: mariadb-stateful
  ports:
    - name: mariadb
      port: 3306
      targetPort: mariadb
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mariadb
spec:
  serviceName: mariadb
  replicas: 1
  selector:
    matchLabels:
      app: mariadb-stateful
  template:
    metadata:
      labels:
        app: mariadb-stateful
    spec:
{{ include "wafflebear.workerScheduling" (dict "app" "mariadb-stateful") | nindent 6 }}
      containers:
        - name: mariadb
          image: "{{ .Values.mariadb.image.repository }}:{{ .Values.mariadb.image.tag }}"
          imagePullPolicy: {{ .Values.mariadb.image.pullPolicy }}
          args:
            - --character-set-server=utf8mb4
            - --collation-server=utf8mb4_unicode_ci
          ports:
            - containerPort: 3306
              name: mariadb
          env:
            - name: TZ
              valueFrom:
                configMapKeyRef:
                  name: {{ include "wafflebear.fullname" . }}-backend-env
                  key: TZ
            - name: MARIADB_DATABASE
              value: web
            - name: MARIADB_USER
              valueFrom:
                configMapKeyRef:
                  name: {{ include "wafflebear.fullname" . }}-backend-env
                  key: DB_ID
            - name: MARIADB_PASSWORD
              valueFrom:
                configMapKeyRef:
                  name: {{ include "wafflebear.fullname" . }}-backend-env
                  key: DB_PASS
            - name: MARIADB_ROOT_PASSWORD
              valueFrom:
                configMapKeyRef:
                  name: {{ include "wafflebear.fullname" . }}-backend-env
                  key: DB_PASS
            - name: MARIADB_ROOT_HOST
              value: "%"
            - name: MARIADB_AUTO_UPGRADE
              value: "1"
          volumeMounts:
            - name: mariadb-data
              mountPath: /var/lib/mysql
          startupProbe:
            exec:
              command:
                - sh
                - -c
                - mariadb-admin --protocol=socket --socket=/run/mysqld/mysqld.sock ping -uroot -p"$MARIADB_ROOT_PASSWORD" --silent
            failureThreshold: 30
            periodSeconds: 5
          readinessProbe:
            exec:
              command:
                - sh
                - -c
                - mariadb-admin --protocol=socket --socket=/run/mysqld/mysqld.sock ping -uroot -p"$MARIADB_ROOT_PASSWORD" --silent
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            exec:
              command:
                - sh
                - -c
                - mariadb-admin --protocol=socket --socket=/run/mysqld/mysqld.sock ping -uroot -p"$MARIADB_ROOT_PASSWORD" --silent
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
{{ toYaml .Values.mariadb.resources | indent 12 }}
  volumeClaimTemplates:
    - metadata:
        name: mariadb-data
      spec:
        accessModes:
          - ReadWriteOnce
        storageClassName: {{ .Values.mariadb.storageClassName | quote }}
        resources:
          requests:
            storage: {{ .Values.mariadb.storage.size | quote }}
