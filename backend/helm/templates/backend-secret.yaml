# Legacy deployment artifact: do not deploy directly. Use devops/Helm as the source of truth.
{{- if .Values.backend.secretEnv }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "wafflebear.fullname" . }}-backend-secret
type: Opaque
stringData:
{{- tpl (toYaml .Values.backend.secretEnv) . | nindent 2 }}
{{- end }}