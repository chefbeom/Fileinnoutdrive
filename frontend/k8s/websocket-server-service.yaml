apiVersion: v1
kind: Service
metadata:
  name: websocket-server-service
spec:
  type: ClusterIP
  selector:
    app: websocket-server
  ports:
    - name: ws
      protocol: TCP
      port: 1234
      targetPort: 1234
